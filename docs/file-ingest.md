# 需求文件读取

更新时间：2026-07-24

## 已完成

- 新增 file:pick 主进程 IPC 和前端 API。
- 新建环境页面支持使用 Electron 原生文件选择器读取需求文件。
- 当前允许 TXT、MD、JSON、LOG，最大 10 MB。
- 路径由原生对话框返回，渲染进程不能直接传入任意本地路径。
- 读取服务包含扩展名、普通文件和文件大小校验。

## 尚未接入

- PDF 文本提取
- DOCX 文本提取
- 图片真实 OCR
- 可替换 OCR provider

PDF/DOCX/图片仍会显示明确的未支持提示，不会伪装成已经完成解析。
